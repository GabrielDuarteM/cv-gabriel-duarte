{
  "name": "cv-gabriel-duarte",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-intl": "^2.4.0",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "ci": "yarn test && yarn build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build",
    "deploy-no-build": "gh-pages -d build"
  },
  "homepage": "https://gabrielduarte.tech",
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
